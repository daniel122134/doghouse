{
  "name": "doghouse",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-front" : "cd frontend && npm install && npm run dev",
    "prod-front" : "cd frontend && npm install && npm run build",
    "server" : "cd backend && npm install && npm run start",
    "all-dev" : "npm-run-all --parallel dev-front server",
    "all-prod" : "npm-run-all --parallel prod-front server"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
